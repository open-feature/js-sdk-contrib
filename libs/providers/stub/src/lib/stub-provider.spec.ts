import { StubProvider } from './stub-provider';

describe('StubProvider', () => {
  it('should be and instance of StubProvider', () => {
    expect(new StubProvider()).toBeInstanceOf(StubProvider);
  });
});
