import { FlagdRESTProvider } from './flagd-rest-provider';

describe('FlagdRESTProvider', () => {
  it('should be and instance of FlagdRESTProvider', () => {
    expect(new FlagdRESTProvider()).toBeInstanceOf(FlagdRESTProvider);
  });
});
