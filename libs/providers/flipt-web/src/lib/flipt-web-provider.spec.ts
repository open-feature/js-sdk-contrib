import { FliptWebProvider } from './flipt-web-provider';

describe('FliptWebProvider', () => {
  it('should be and instance of FliptWebProvider', () => {
    expect(new FliptWebProvider()).toBeInstanceOf(FliptWebProvider);
  });
});
