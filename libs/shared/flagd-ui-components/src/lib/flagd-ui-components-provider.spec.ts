import { FlagdUiComponentsProvider } from './flagd-ui-components-provider';

describe('FlagdUiComponentsProvider', () => {
  it('should be and instance of FlagdUiComponentsProvider', () => {
    expect(new FlagdUiComponentsProvider()).toBeInstanceOf(FlagdUiComponentsProvider);
  });
});
