# Add files here to ignore them from prettier formatting

dist/
coverage/
node_modules/
.nx/cache/
.nx/workspace-data/

CHANGELOG.md
package-lock.json

.nx/self-healing

# The open-feature/spec submodule the provider TCK reads its conformance artifacts from. They are
# consumed byte for byte by every language's provider TCK and are owned upstream, so reformatting
# them here would fork the definition of conformance -- and would dirty the submodule besides.
libs/shared/provider-tck/spec/
