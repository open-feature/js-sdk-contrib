import { <%= libClassName %> } from './<%= libFileName %>';

describe('<%= libClassName %>', () => {
  it('should be and instance of <%= libClassName %>', () => {
    expect(new <%= libClassName %>()).toBeInstanceOf(<%= libClassName %>);
  });
});